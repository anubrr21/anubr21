name: Update Profile Views

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  update-views:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Update Profile Views
        run: |
          curl -X GET "https://api.countapi.xyz/hit/Anubrata-dev/views"
